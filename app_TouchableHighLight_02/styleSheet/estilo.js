import { StyleSheet } from "react-native";

export const estilos = StyleSheet.create({
  conteudo: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    padding: 30
  },

  titulo: {
    fontSize: 35,
    color: "darkred",
    marginBottom: 30
  }
});